{
    "extends": "./tsconfig.json",
    // 👇️ this may vary depending on how you
    // name your test files
    "exclude": [
        "tests/**/*.test.ts",
        "src/**/__mocks__/**/*.ts",
        "node_modules",
    ]
}